# SG23 UDFRC Gmsh Example

This example generates a 2D square fiber-matrix structure genome mesh with physical groups for matrix and fiber materials and writes the result to `sg2d_fm_square.msh`.

## Dependencies

Install only this example's dependencies from inside this directory:

```bash
uv sync
```

## Run

```bash
uv run python build_sg.py
```
