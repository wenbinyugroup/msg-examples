# Gmsh Periodic Mesh Example

This example is a minimal script showing how to impose periodic constraints with the Gmsh Python API and export the resulting mesh to `periodic.msh`.

## Dependencies

Install only this example's dependencies from inside this directory:

```bash
uv sync
```

## Run

```bash
uv run python periodic.py
```
