[project]
name = "msg-examples"
version = "0.1.0"
description = "Repository-level tooling for the MSG examples collection"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mystmd",
]

[dependency-groups]
dev = [
    "ruff",
]
